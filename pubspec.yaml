name: steadpay_flutter
description: Native Flutter enforcement SDK for Steadpay billing states.
version: 1.0.0
homepage: https://github.com/steadpay/steadpay-flutter

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  url_launcher: ^6.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

